use rupa_vnode::VNode;

/// Utilities for UI snapshot verification.
pub struct Snapshot;

impl Snapshot {
    /// Asserts that a VNode matches a stored snapshot.
    pub fn assert_match(_node: &VNode, _name: &str) {
        // Placeholder for snapshot comparison logic
    }
}
