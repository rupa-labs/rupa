# Module: CLI Commands (`crates/rupa-cli/src/commands.rs`) 🛠️

This module implements the core subcommands for the Rupa CLI tool.

---

## 🏗️ Commands
- `new`: Scaffolds a new workspace using `templates`.
- `dev`: Starts the development server with hot-reloading support.
- `build`: Invokes the `bundler` for the specified target platform.
- `doctor`: Diagnoses the local environment for missing dependencies (Android NDK, etc.).
