# Radio 🔘

A form control for selecting a single option from a mutually exclusive set.

---

## 🏗️ Purpose
`Radio` buttons ensure that only one choice can be active at a time within a group.

---

## 🚀 Example
```rust
Radio::new("Option A").selected(true)
```
