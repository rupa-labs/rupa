# Switch ⏻

A modern, high-visibility binary toggle.

---

## 🏗️ Purpose
`Switch` is preferred over Checkbox for immediate, high-impact settings (e.g., "Enable GPU", "Airplane Mode").

---

## 🚀 Example
```rust
Switch::new().active(is_enabled_signal)
```
